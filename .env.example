# Server
PORT=3000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=@1234Strong!
DB_NAME=db

# JWT (use a long random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# Uploads (optional: use ./uploads if not set)
UPLOAD_DIR=./uploads

